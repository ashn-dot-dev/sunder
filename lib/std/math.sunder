namespace std;
import "std/int.sunder"; # RM ME AFTER DEBUGGING

func min[[:T]](x: T, y: T) T {
    if x < y {
        return x;
    }
    return y;
}

func max[[:T]](x: T, y: T) T {
    if x > y {
        return x;
    }
    return y;
}
