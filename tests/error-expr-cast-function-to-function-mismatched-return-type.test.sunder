func f(x: usize, y: ssize) void { }
let g = (:func(usize, ssize) usize)f;
################################################################################
# [error-expr-cast-function-to-function-mismatched-return-type.test.sunder:2] error: cannot convert from `func(usize, ssize) void` to `func(usize, ssize) usize` (mismatched return types `void` and `usize`)
# let g = (:func(usize, ssize) usize)f;
#         ^
