import "std";

alias i32 = s32;
var a: i32 = 123;
var b: i32 = 123s32;

alias signed32 = s32;
const c = (:signed32)123s;

alias standard = std;
alias writer_result = standard::result[[usize, []byte]];

alias println = standard::print_line;

alias foo = s64;

func main() void {
    dump a;
    dump b;
    dump c;

    alias foo = s32;

    var r = writer_result::init_error("foo");
    println(std::out(), r.error());
}
################################################################################
# 7B 00 00 00
# 7B 00 00 00
# 7B 00 00 00
# foo
