let foo: ssize = 1s + true;
################################################################################
# [error-expr-binary-arithmetic-invalid-xhs.test.sunder:1] error: invalid arguments of types `ssize` and `bool` in binary `+` expression
# let foo: ssize = 1s + true;
#                     ^
