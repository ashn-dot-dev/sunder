import "std/core.sunder";
import "sys";

func main() void {
    var a: std::optional[[u16]] = std::optional[[u16]]::init_value(0xBEEF);
    std::assert(a.is_value());
    std::assert(not a.is_empty());
    sys::dump[[u16]](a.value());
}
################################################################################
# EF BE
