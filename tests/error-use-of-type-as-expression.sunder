const foo: ssize = usize;
################################################################################
# [error-use-of-type-as-expression.sunder:1] error: use of type `usize` as an expression
# const foo: ssize = usize;
#                    ^
