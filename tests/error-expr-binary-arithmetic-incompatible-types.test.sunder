let foo: ssize = 1s + 2u;
################################################################################
# [error-expr-binary-arithmetic-incompatible-types.test.sunder:1] error: invalid arguments of types `ssize` and `usize` in binary `+` expression
# let foo: ssize = 1s + 2u;
#                     ^
