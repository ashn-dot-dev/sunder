let foo: ssize = usize;
################################################################################
# [error-use-of-type-as-expression.test.sunder:1] error: use of type `usize` as an expression
# let foo: ssize = usize;
#                  ^
