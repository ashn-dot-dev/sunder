import "std/core.sunder";
import "std/fs.sunder";

const path: []byte = "data.tmp";

func main() void {
    var result: std::result[[std::file, []byte]] = std::file::open(path, std::file::OPEN_WRITE | std::file::OPEN_APPEND);
    std::print_line(std::err(), result.error());
}
################################################################################
# Invalid argument
