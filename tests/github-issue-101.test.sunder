import "std";

struct foo {
    var x: u32;
    var y: u32;
    const C: foo = (:foo){};
    var z: u32;
}

func main() void {
    dump sizeof(foo);
}
################################################################################
# [github-issue-101.test.sunder:6] error: struct `foo` contains a member constant of incomplete struct type `foo`
#     const C: foo = (:foo){};
#     ^
