import "std/io.sunder";

func main() void {
    std::print_line(
        std::writer::init[[:std::stream]](&std::out),
        "Hello, world!");
}
################################################################################
# Hello, world!
