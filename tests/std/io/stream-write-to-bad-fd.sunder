import "std/io.sunder";
import "std/writer.sunder";

func main() void {
    var stream: std::stream = (:std::stream){.fd = 0xBEEFCAFE};
    var res: std::result[[:usize, :[]byte]] = stream.write("nope");

    std::out.write("error: ");
    std::out.write(res.error());
    std::out.write("\n");
}
################################################################################
# error: Bad file descriptor
