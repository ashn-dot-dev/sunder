import "std/core.sunder";
import "std/fs.sunder";

const path: []byte = "data.tmp";

func main() void {
    var result: std::result[[std::fs::file, []byte]] = std::fs::file::open(path, std::fs::OPEN_WRITE | std::fs::OPEN_APPEND);
    std::print_line(std::err(), result.error());
}
################################################################################
# Invalid argument
