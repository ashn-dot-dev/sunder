import "std/io.sunder";
import "std/sys.sunder";
import "sys/sys.sunder";

func main() void {
    var error: std::sys::error = (:std::sys::error){.code = sys::ENOENT};
    std::print_line(std::out(), error.message());
}
################################################################################
# No such file or directory
