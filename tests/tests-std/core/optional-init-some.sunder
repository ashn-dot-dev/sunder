import "std/core.sunder";

func main() void {
    var a: std::optional[[:u16]] = std::optional[[:u16]]::init_some(0xBEEF);
    dump a.is_some();
    dump a.is_none();
    dump a.unwrap();
}
################################################################################
# 01
# 00
# EF BE
