import "std/core.sunder";
import "std/ascii.sunder";

func main() void {
    dump std::ascii::is_space(' ');
    dump std::ascii::is_space('\t');
    dump std::ascii::is_space('\n');
    dump std::ascii::is_space(0x0B);
    dump std::ascii::is_space(0x0C);
    dump std::ascii::is_space(0x0D);

    dump void::init();

    dump std::ascii::is_space('A');
    dump std::ascii::is_space(0x7F);
    dump std::ascii::is_space(0xFF);
}
################################################################################
# 01
# 01
# 01
# 01
# 01
# 01
#
# 00
# 00
# 00
