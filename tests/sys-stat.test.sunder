import "std";
import "sys";

func main() void {
    var statbuf: sys::stat_type = std::zeroed[[sys::stat_type]]();

    var sysret = sys::stat(&"sys-stat.test.sunder\0"[0], &statbuf);
    std::assert(sysret == 0);

    dump sys::S_ISREG(statbuf.st_mode);
    dump sys::S_ISDIR(statbuf.st_mode);
    dump sys::S_ISLNK(statbuf.st_mode);
}
################################################################################
# 01
# 00
# 00
