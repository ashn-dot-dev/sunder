import "std";

struct foo {
    var x: u32;
    var y: u32;
    const C: foo = (:foo){};
    var z: u32;
}

func main() void {
    dump sizeof(foo);
}
################################################################################
# [github-issue-101-explicit-type.test.sunder:6] error: struct type `foo` is incomplete
#     const C: foo = (:foo){};
#                    ^
