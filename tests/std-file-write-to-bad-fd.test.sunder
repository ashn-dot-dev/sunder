import "std/core.sunder";
import "std/fs.sunder";

func main() void {
    var file = (:std::file){._fd = 0xBEEFCAFE};
    var result = file.write("nope");
    std::print_line(std::out(), result.error().*.data);
}
################################################################################
# [system error EBADF] Bad file descriptor
