import "std/page_allocator.sunder";
import "std/vector.sunder";
import "sys";

func main() void {
    var vec: std::vector[[u32]] = std::vector[[u32]]::init(std::allocator::init[[std::page_allocator]](std::page_allocator::the()));
    sys::dump[[usize]](vec.count());
    sys::dump[[usize]](vec.capacity());

    vec.resize(0);
    sys::dump[[usize]](vec.count());
    sys::dump[[usize]](vec.capacity());

    vec.resize(1);
    sys::dump[[usize]](vec.count());
    sys::dump[[usize]](vec.capacity());

    vec.resize(0);
    sys::dump[[usize]](vec.count());
    sys::dump[[usize]](vec.capacity());

    vec.resize(1);
    sys::dump[[usize]](vec.count());
    sys::dump[[usize]](vec.capacity());

    vec.resize(2);
    sys::dump[[usize]](vec.count());
    sys::dump[[usize]](vec.capacity());

    vec.resize(0xBEEF);
    sys::dump[[usize]](vec.count());
    sys::dump[[usize]](vec.capacity());

    vec.resize(0);
    sys::dump[[usize]](vec.count());
    sys::dump[[usize]](vec.capacity());

    vec.fini();
}
################################################################################
# 00 00 00 00 00 00 00 00
# 00 00 00 00 00 00 00 00
# 00 00 00 00 00 00 00 00
# 00 00 00 00 00 00 00 00
# 01 00 00 00 00 00 00 00
# 01 00 00 00 00 00 00 00
# 00 00 00 00 00 00 00 00
# 01 00 00 00 00 00 00 00
# 01 00 00 00 00 00 00 00
# 01 00 00 00 00 00 00 00
# 02 00 00 00 00 00 00 00
# 02 00 00 00 00 00 00 00
# EF BE 00 00 00 00 00 00
# EF BE 00 00 00 00 00 00
# 00 00 00 00 00 00 00 00
# EF BE 00 00 00 00 00 00
