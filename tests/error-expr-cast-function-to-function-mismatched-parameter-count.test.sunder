func f(x: usize, y: ssize) void { }
let g = (:func(usize) void)f;
################################################################################
# [error-expr-cast-function-to-function-mismatched-parameter-count.test.sunder:2] error: cannot convert from `func(usize, ssize) void` to `func(usize) void` (mismatched parameter count)
# let g = (:func(usize) void)f;
#         ^
