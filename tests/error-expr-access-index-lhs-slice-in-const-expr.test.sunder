const array: [3u]ssize = (:[3u]ssize)[1s, 3s, 5s];
const slice: []ssize = (:[]ssize){&array[1u], 2u};
const x: ssize = slice[1u];
################################################################################
# [error-expr-access-index-lhs-slice-in-const-expr.test.sunder:3] error: indexing with left-hand-type `[]ssize` not supported in compile-time expressions
# const x: ssize = slice[1u];
#                       ^
