import "std/core.sunder";
import "std/null_allocator.sunder";
import "std/vector.sunder";

func main() void {
    var vec: std::vector[[u32]] = std::vector[[u32]]::init(std::allocator::init[[std::null_allocator]](std::null_allocator::the()));
    vec.fini();
}
