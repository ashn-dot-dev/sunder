import "std/core.sunder";
import "std/vector.sunder";
import "util/allocation_failure_allocator.sunder";

func main() void {
    var vec: std::vector[[u32]] = std::vector[[u32]]::init(std::allocator::init[[util::allocation_failure_allocator]](util::allocation_failure_allocator::the()));
    vec.fini();
}
