# only SUNDER_BACKEND=C
func main() void {
    let a = 256.0f64;
    (:u8)a;
}
################################################################################
# fatal: arithmetic operation produces out-of-range result
