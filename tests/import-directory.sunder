import "std";
import "import";

func main() void {
    dump a;
    dump b();
    c = c + 1u16;
    dump c;

    std::print_line(
        std::writer::init[[:std::stream]](&std::out),
        "imported from the standard library");
}
################################################################################
# AA AA
# BB BB
# CC CC
# imported from the standard library
