import "std";

func main() void {
    var v = void::VALUE;
    std::print_format_line(
        std::out(),
        "{}",
        (:[]std::formatter)[std::formatter::init[[void]](&v)]);
}
################################################################################
# void
