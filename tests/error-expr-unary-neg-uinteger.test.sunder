func main() void {
    var x: usize = 123u;
    dump -x;
}
################################################################################
# [error-expr-unary-neg-uinteger.test.sunder:3] error: invalid argument of type `usize` in unary `-` expression
#     dump -x;
#          ^
